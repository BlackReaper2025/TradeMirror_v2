# TradeMirror Rules

- Only perform the exact task requested.
- Only modify the specified file.
- Do not change anything else.

- Show only the changed code, not the full file.

- Do not use margin hacks (marginRight, marginLeft, translateY, width expansion).
- Use proper grid layout and clean structure.

- Preserve all existing functionality unless explicitly told otherwise.

- The very last line of every response must be exactly:
DONE